# Local Image Manager

Obsidian 插件：粘贴图片时，自动保存到本地，按笔记路径存放，并按标题层级自动命名（如 `1.2.3-1.png`）。

**纯本地存储，无需网络，无任何第三方服务依赖。**

---

## ✨ 功能特性

- 📋 **粘贴即保存**：从剪贴板粘贴图片（截图、复制图片文件）时自动处理。
- 🗂️ **按笔记路径存放**：图片保存在 `基础文件夹/笔记目录/笔记名/` 下，保持与笔记的关联。
- 🔢 **智能命名**：根据光标所在的标题层级生成带序号的名称（如 `1.2-3.png`），类似 Word 多级列表。
- 🔄 **自动计数**：每个标题层级下的图片序号从 1 开始递增，重启 Obsidian 后依然保持。
- ⚙️ **灵活配置**：可自定义基础存储文件夹、最大标题深度、粘贴行为（总是保存/每次询问）。
- 📝 **无缝插入**：自动在编辑器中插入 `![[相对路径]]` 链接，无需手动调整。

---

## 📦 安装

### 手动安装
1. 在 Obsidian 仓库的 `.obsidian/plugins/` 目录下新建文件夹 `local-image-manager`。
2. 下载 [`main.js`](main.js) 和 [`manifest.json`](manifest.json) 放入该文件夹。
3. 在 Obsidian 设置 → 社区插件 → 已安装，启用 **Local Image Manager**。

### 通过 BRAT 安装
（如果你将插件发布到 GitHub，可配置 BRAT 安装）

---

## ⚙️ 配置说明

在插件设置中可调整：

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| **基础存储文件夹** | 图片将保存在此文件夹下，后接笔记的目录和文件名（例如 `Assets/Image/DL/ANN/`）。 | `Assets/Image` |
| **标题层级最大深度** | 生成文件名时最多使用几级标题序号（1-6）。超出部分将被截断。 | `6` |
| **粘贴图片行为** | `总是保存`：粘贴时自动保存；`每次询问`：弹出确认窗口。 | `总是保存` |

---

## 🚀 使用方法

### 基本流程
1. 打开一篇带有标题的笔记（例如 `DL/ANN.md`，包含 `#`、`##` 等标题）。
2. 将光标放在某个标题下方（该位置决定了图片的命名层级）。
3. 粘贴图片（`Ctrl+V` / `Cmd+V`）或截图后粘贴。
4. 插件会自动：
   - 计算当前标题的编号（如 `1.2.3`）。
   - 在该层级下递增计数器（如 `1.2.3-1.png`）。
   - 将图片保存到 `Assets/Image/DL/ANN/` 目录（如果 `基础存储文件夹` 设为 `Assets/Image`）。
   - 在光标处插入 `![[相对路径]]`。

### 示例
假设笔记 `DL/ANN.md` 内容如下：
```markdown
# 第一章
## 1.1 引言
### 1.1.1 背景
（光标在此行）